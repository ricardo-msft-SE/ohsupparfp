{
  "routes": [
    {
      "route": "/teams/config",
      "rewrite": "/teams/config.html"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "*.{css,js,ico,png,jpg,gif,svg,json,html}"]
  }
}
