name: ohsupparfp-rfp-approver
metadata:
  template: custom
services:
  web:
    project: .
    host: containerapp
    language: python
    docker:
      path: ./Dockerfile
infra:
  provider: bicep
  path: ./infra
